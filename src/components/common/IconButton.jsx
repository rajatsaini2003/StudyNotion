import React from 'react'

const IconButton = ({
    text,
    onClick,
    children,
    disabled,
    outline=false,
    customClasses,
    type
}) => {
  return (
    <div>
      
    </div>
  )
}

export default IconButton
